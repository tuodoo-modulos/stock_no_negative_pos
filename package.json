{
  "name": "stock_no_negative_pos",
  "version": "1.1.7",
  "description": "The stock_no_negative_pos module makes the following improvements.",
  "main": "standard-version-updater.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version && git push --follow-tags"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
